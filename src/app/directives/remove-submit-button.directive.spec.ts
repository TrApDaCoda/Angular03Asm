import { RemoveSubmitButtonDirective } from './remove-submit-button.directive';

describe('RemoveSubmitButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveSubmitButtonDirective();
    expect(directive).toBeTruthy();
  });
});
